packages:
  "recharts": "For beautiful dashboard analytics charts"
  "date-fns": "For formatting dates in pt-BR"
  "framer-motion": "For smooth page transitions and micro-interactions"
  "clsx": "For merging tailwind classes conditionally"
  "tailwind-merge": "For merging tailwind classes without conflicts"

images:
  - path: "artifacts/retail-app/public/images/auth-bg.png"
    description: "Abstract warm gradient background with soft orange, amber and peach tones, professional minimal"
    aspect_ratio: "16:9"

notes:
  - "Assuming standard REST endpoints are available as defined in the generated API client."
  - "App is built mobile-first with a responsive sidebar layout."
